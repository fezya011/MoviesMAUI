<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FitnessMAUI.Views.StudiosPage"
             Title="Студии">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Добавить" Command="{Binding AddStudioCommand}" />
    </ContentPage.ToolbarItems>

    <RefreshView IsRefreshing="{Binding IsRefreshing}" Command="{Binding RefreshCommand}">
        <CollectionView ItemsSource="{Binding Studios}" SelectionMode="Single" SelectedItem="{Binding SelectedStudio}">
            <CollectionView.Header>
                <Label Text="Все студии" 
                       FontSize="24"
                       FontAttributes="Bold"
                       TextColor="White"
                       Margin="15,10"/>
            </CollectionView.Header>

            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <SwipeView>
                        <SwipeView.LeftItems>
                            <SwipeItems>
                                <SwipeItem Text="Удалить" 
                                           BackgroundColor="Red" 
                                           Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.DeleteStudioCommand}"
                                           CommandParameter="{Binding .}"/>
                            </SwipeItems>
                        </SwipeView.LeftItems>

                        <Grid ColumnSpacing="15" HeightRequest="80" Padding="15,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <VerticalStackLayout Grid.Column="0" Spacing="2" VerticalOptions="Center">
                                <Label Text="{Binding Name}" 
                                       TextColor="White"
                                       FontSize="16"
                                       FontAttributes="Bold"/>
                                <Label Text="{Binding DirectorFullName}" 
                                       TextColor="#BDC3C7"
                                       FontSize="12"/>
                            </VerticalStackLayout>

                            <Frame Grid.Column="1"
                                   BackgroundColor="#3498DB"
                                   CornerRadius="8"
                                   Padding="10,5">
                                <Label Text="{Binding Rating}" 
                                       TextColor="White"
                                       FontSize="14"
                                       FontAttributes="Bold"/>
                            </Frame>
                        </Grid>
                    </SwipeView>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </RefreshView>
</ContentPage>